print "SAT"

func contain_s lst obj (
    = len size list
    print "hledam"
    print len
    print obj

    = idx 0
    loop (
        if == idx len ( end )

        = elem at list idx
        if == elem obj (
            print "nalezeno na " print idx
            return yes
        )

        += idx 1
    )

    return no
)

= list [ 1 2 3 4 5 ]

print contain_s list 3

return "ahoj"